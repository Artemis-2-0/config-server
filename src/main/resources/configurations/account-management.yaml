server:
  port: 6093
spring:
  neo4j:
    uri: ${NEO4J_URI:bolt://localhost:7688}
    authentication:
      username: ${NEO4J_USERNAME:neo4j}
      password: ${NEO4J_PASSWORD:password}
  data:
    neo4j:
      database: artemis.account
logging:
  file:
    name: logs/account-management/spring-boot.log